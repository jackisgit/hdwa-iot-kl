# 环境配置
spring:
  profiles:
    active: jinyang_dingyang

# web访问端口号
server:
  port: 8111

# mybatis 配置
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# 采集器配置
epc:
  gatewayId: KL
  subsystem: klxt/
  threadNum: 30
  cron: 30 0/1 * * * ?

beanName: passengerFlowDevice

full-sync:
  cron: 15 0/4 * * * ?
